{
  "name": "969-project",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "start-backend": "cd backend && npm start"
  }
}
